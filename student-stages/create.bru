meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{protocol}}://{{host}}/{{version}}/students/1/stages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "stage_id": 1,
    "awarded_on": "2025-01-31"
  }
}
