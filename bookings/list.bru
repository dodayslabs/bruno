meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{protocol}}://{{host}}/{{version}}/bookings?filter[provider]=1&filter[student]=1&filter[ended]=0&include=class,sessions.session
  body: none
  auth: bearer
}

params:query {
  filter[provider]: 1
  filter[student]: 1
  filter[ended]: 0
  include: class,sessions.session
}

auth:bearer {
  token: {{token}}
}
