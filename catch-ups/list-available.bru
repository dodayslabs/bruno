meta {
  name: list
  type: http
  seq: 1
}

get {
  url: {{protocol}}://{{host}}/{{version}}/available-catch-up-sessions?filter[banked_session]=1
  body: none
  auth: bearer
}

params:query {
  filter[banked_session]: 1
}

auth:bearer {
  token: {{token}}
}
