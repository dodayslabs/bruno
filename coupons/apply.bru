meta {
  name: apply
  type: http
  seq: 1
}

post {
  url: {{protocol}}://{{host}}/{{version}}/coupons/apply
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
    {
        "class": 1,
        "code": "TEN"
    }
}
