meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{protocol}}://{{host}}/{{version}}/students/1/goals
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "goal_id": 1
  }
}
